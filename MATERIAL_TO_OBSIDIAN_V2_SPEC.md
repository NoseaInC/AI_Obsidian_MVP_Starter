# Material to Obsidian V2

```text
message / PDF / URL / pasted text / current note
→ InputBundle
→ ConversationFocus + pronoun resolution
→ explicit assistant intent
→ MaterialUnderstanding + KnowledgeUnits
→ TargetNoteResolver
→ OrganizationPlan
→ policy check
→ snapshot + atomic managed write OR Change Set
→ verification + audit + Undo
```

The Assistant is the universal input. Materials, Review, Plan and Today remain projections of resulting Artifacts. An explicit “整理到 Obsidian” is write authorization for bounded low-risk work in high-autonomy mode; it is not authorization to overwrite reviewed/core, delete, move, rename or apply a real Prepared PDF bundle.

The Delta Method acceptance creates or safely updates one method note containing assumptions, one- and multivariate formulas, Taylor/Slutsky derivation, examples, limitations, links and provenance. It must not create an unrelated learning pack.

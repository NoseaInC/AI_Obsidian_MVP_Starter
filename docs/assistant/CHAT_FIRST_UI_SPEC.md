# Chat-first UI 规格

## 导航

左侧固定为知序、今日、资料、审核、计划、助手；底部保留设置、帮助和本地 Agent 在线状态。默认完整工作区打开助手。Ribbon 单击打开快速侧栏，双击打开助手。

## 助手

- 顶部：标题、模型、历史、新会话、设置；
- 中部：会话消息、执行时间线和 Artifact 卡片；
- 底部：固定统一输入框；
- 输入：拖放、粘贴文件、粘贴 URL、附件按钮、当前笔记、`@`、`/`；
- 写入提示：所有写操作先生成 Change Set。

## 资料

一列状态中心。每行展示标题、来源、更新时间、结构化成果数、状态和进度；内部 ID 只在详情中显示。筛选为全部、待确认、处理中、研究包、已完成、失败和历史。

## 审核

三栏：草稿列表、Markdown/提案预览、证据与风险。底部固定拒绝、稍后、退回助手调整、修改后接受。窄屏隐藏 Inspector，不覆盖主内容。

## 计划

今天/未来两天/本周/周末/所有标签；顶部展示可用时间、主支线比例、任务数和预计完成率；正文展示今日学习块与周末预览。

## 今日

左侧五组队列，右侧一个当前决策。详情包含预计时间、难度、置信度、推荐原因、学习目标和开始/暂不学习操作。

## 响应式与可访问性

- 900px 以下导航缩成图标栏；
- 760px 以下今日进入列表/详情分层导航；
- 模型配置 Drawer 在窄屏覆盖右侧并支持 Esc；
- 所有按钮有 aria-label、focus-visible、键盘 Enter/方向键；
- reduced motion 禁用动效；
- 所有样式限制在 `.la-` 根节点。


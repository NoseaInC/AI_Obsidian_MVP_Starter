下面这份可以直接作为 **`UI_V3_SPEC.md`** 交给 Codex。重点不是“照着截图大概做”，而是明确每个区域的尺寸、职责、滚动归属、交互和状态，避免它继续自由发挥。

# 知序 / Learning Agent UI V3 详细规范

## 一、产品整体结构

### 1. 产品仍然以 Obsidian 为主体

Learning Agent 是一个 Obsidian Workspace View，不是独立应用，也不替代 Obsidian 原生文件树。

Obsidian 自身负责：

* 左侧文件浏览器；
* 标签页；
* 编辑器；
* 全局搜索；
* 原生设置与主题。

Learning Agent 工作区内部负责：

* 今日推荐；
* 资料处理；
* 知识审核；
* 学习计划；
* AI 助手。

禁止在插件内部复制一套 Vault 文件树。

---

## 二、整体页面骨架

Learning Agent 主工作区使用固定高度布局：

```text
┌──────────────────────────────────────────────────────────────┐
│ Global Header                                                │ 52px
├────────────┬─────────────────────────────────────────────────┤
│ Module Nav │ Module Content                                  │
│ 188px      │                                                 │
│            │                                                 │
│            │                                                 │
└────────────┴─────────────────────────────────────────────────┘
```

### 1. Global Header

高度固定为 `52px`，不随内容滚动。

内容：

```text
知序 / 学习 Agent                     搜索  日历  刷新  设置
```

左侧：

* 插件图标；
* 当前产品名称；
* 当前模块名称，可省略重复标题。

右侧：

* 全局搜索；
* 日历入口；
* 刷新；
* 设置；
* 更多菜单。

禁止：

* 在 Header 下再放一个占据大量高度的五标签导航；
* 每个页面重复显示“大标题 + 副标题 + 工具栏”；
* 左右箭头单独占一整行。

内部历史返回按钮只在需要时出现在内容工具栏内。

---

### 2. Module Nav

宽度：

* 正常：`188px`
* 紧凑：`56px`
* 小于 900px 时自动折叠。

包含：

```text
学习 Agent
● 在线 · 协议 v1

今日
资料
审核
计划
助手
```

下方是全局统计摘要：

```text
今日进度       60%
今日复习       3 项 · 10 分钟
今日学习       1 项 · 15 分钟
待确认资料     1 项
待审核知识     4 项
运行任务       1 项
失败任务       0 项
```

最底部：

```text
输入问题或使用 / 命令…
设置
```

规则：

* 当前模块使用轻微背景和 Accent Color 图标；
* 不使用大面积蓝色块；
* 不展示完整 ID、hash、绝对路径；
* 每个统计项可点击跳转到对应模块；
* 本地服务离线时，在顶部显示可操作的错误，而不是整个页面失效。

---

## 三、滚动规则

这是当前实现最需要纠正的部分。

### 总原则

**整个 Learning Agent 页面本身不得整体垂直滚动。**

外层：

```css
height: 100%;
overflow: hidden;
```

每个需要滚动的子区域独立设置：

```css
min-height: 0;
overflow-y: auto;
```

### 固定区域

以下区域始终固定：

* Global Header；
* Module Nav；
* 模块顶部工具栏；
* 详情页底部主要操作栏；
* AI 助手输入框；
* 审核页接受/拒绝操作栏；
* 计划页底部统计栏。

### 可滚动区域

* 推荐列表；
* 推荐详情正文；
* 资料列表；
* Change Set 详情；
* 审核列表；
* Markdown 预览；
* 来源证据 Inspector；
* 计划任务列表；
* 聊天消息；
* 模型设置 Drawer 内容。

禁止最外层出现一条贯穿整个页面的滚动条。

---

# 四、模块一：今日 / 推荐中心

## 1. 布局

完整宽度时：

```text
┌──────────────┬────────────────────────┬────────────────┐
│ 推荐列表      │ 推荐详情                │ 今日统计        │
│ 330px         │ minmax(460px, 1fr)     │ 270px           │
└──────────────┴────────────────────────┴────────────────┘
```

宽度不足 `1180px`：

* 隐藏今日统计；
* 顶部显示“统计”按钮；
* 点击后从右侧打开 Drawer。

宽度不足 `860px`：

* 推荐列表和详情不能并排；
* 点击列表项进入详情；
* 顶部返回按钮返回列表。

---

## 2. 推荐列表

顶部工具栏固定：

```text
推荐中心                         筛选
[搜索推荐内容……]
[全部] [复习] [学习] [探索] [资料]
```

排序菜单：

* 智能排序；
* 最需要复习；
* 主线优先；
* 时间最短；
* 最近加入；
* 兴趣探索。

推荐行高度约 `112–136px`。

每行显示：

```text
[图标] 倾向得分充分性                   [收藏]
       复习 · 12 分钟
       重要度 3/5 · 掌握度 0/4
       适合作为当前因果推断路线的下一步。
```

颜色：

* 复习：绿色；
* 学习：蓝色；
* 探索：紫色；
* 资料：橙色。

颜色只用于图标、小标签和细边框，不允许整张卡片高饱和着色。

选中状态：

* Accent Color 细边框；
* 极浅的 Accent 背景；
* 标题仍使用正常文字颜色；
* 不要把标题染成粉红色。

底部固定：

```text
共 12 项推荐                     刷新
```

---

## 3. 推荐详情

顶部：

```text
[图标] 倾向得分充分性              收藏  ⋯
       学习 · 12 分钟 · 重要度高
```

正文按以下顺序：

### 为什么推荐

展示 2–4 条结构化理由：

```text
• 当前路线相关性：统计 / 机器学习主线
• 掌握度 0/4，重要度 3/5
• 是理解因果效应估计的关键前置知识
```

禁止只写泛化文案：

> “适合作为下一步，建议巩固。”

必须说明推荐依据。

### 前置知识

以 chips 或列表显示：

```text
条件独立性
倾向得分定义
无混杂性
```

点击：

* 打开对应笔记；
* 没有正式笔记时，打开微知识预览。

### 相关笔记

每项显示：

* 笔记标题；
* 类型；
* 最近修改时间；
* 打开按钮。

禁止显示 `dragonnet-938e97f334d2` 作为面向用户的唯一标题，应显示：

```text
Dragonnet 论文整理
```

技术 ID 放进“技术详情”。

### 小测试预览

显示一题，不立即展示答案：

```text
在什么条件下，倾向得分能够平衡处理组与对照组？
```

操作：

```text
查看提示
开始测试
```

### 相关小知识点

可点击 chips：

```text
平衡性质
标准化差异
Love Plot
协变量平衡检验
IPW 权重
```

点击后显示 Popover：

```text
平衡性质
倾向得分相同的样本，其协变量分布在处理组和对照组之间应趋于一致。

为什么相关：它是倾向得分方法有效的关键依据。
预计时间：5 分钟

[立即了解] [加入明天] [加入周末] [不感兴趣]
```

---

## 4. 底部操作栏

固定在详情底部：

```text
[稍后] [加入明天] [加入周末] [不感兴趣]    [开始学习 · 12 分钟]
```

主按钮始终在最右侧。

按钮层级：

* 主按钮：Accent Color；
* 次按钮：普通边框；
* 负反馈“不感兴趣”：无红色，使用普通文本；
* 高风险操作才使用红色。

---

## 5. 今日统计 Inspector

固定宽度约 `270px`。

### 今日进度

```text
今日统计

     60%
25 / 40 分钟
```

环形进度下方：

```text
已完成     25 分钟
剩余       15 分钟
主线/支线  70% / 30%
```

### 摘要行

```text
今日复习        3 项 · 10 分钟
今日学习        1 项 · 15 分钟
待确认资料      1 项
待审核知识      4 项
失败任务        0 项
```

全部可点击。

### 当前任务

```text
正在准备：Dragonnet 论文
提取证据 · 64%
[查看任务]
```

没有任务时：

```text
当前没有运行任务
[导入资料]
```

### 快速助手

底部：

```text
输入问题或使用 / 命令…
```

按 Enter 发送，点击展开完整助手。

---

# 五、模块二：资料 / 处理中心

## 1. 布局

```text
┌──────────────┬────────────────────────┬────────────────┐
│ 资料列表      │ Change Set 详情         │ 状态 Inspector │
│ 330px         │ minmax(500px, 1fr)     │ 270px           │
└──────────────┴────────────────────────┴────────────────┘
```

---

## 2. 资料列表

顶部固定：

```text
[待确认 2] [处理中 1] [已应用 7] [失败 1] [历史]
[搜索资料……] [类型] [来源] [时间]
```

资料列表项：

```text
Dragonnet 论文
论文 · PDF
等待确认 · 87%
DeepSeek V4 Pro
12 分钟前
```

处理中项目显示进度条。

失败项目：

```text
与同学讨论：扩散模型
微信导入
失败：解析超时
昨天 18:42
```

操作菜单：

* 查看详情；
* 重试；
* 编辑参数后重试；
* 查看日志；
* 删除失败记录。

规则：

* `applied` 不出现在待确认；
* `failed` 不算运行中；
* 不使用 prepared_id 作为标题；
* 内部 ID 只出现在技术详情中。

---

## 3. Change Set 详情

顶部：

```text
Dragonnet 论文
论文 · PDF                等待确认
```

### Change Set 概览

四个紧凑统计块：

```text
待创建 3
待更新 2
建议更新 1
将跳过 4
```

### 关键变更

```text
新增概念
• 倾向得分充分性
• 非参数估计方程

更新笔记
• Dragonnet 论文整理

更新建议
• 观测数据中的平均处理效应估计
```

每一项可展开：

* 目标路径；
* 内容预览；
* 证据页码；
* 选择理由；
* 是否创建。

### 目标位置

显示人类可读目录：

```text
概念        20-Knowledge/Concepts
主题        20-Knowledge/Topics
来源        10-Sources/Papers
```

### 证据支持

```text
第 3 页   方法论框架         可信度 92%
第 6 页   训练数据与规模     可信度 88%
第 12 页  实验结果           可信度 90%
```

点击页码打开 PDF。

---

## 4. 状态 Inspector

### 处理进度

```text
处理状态

87%
步骤 3 / 4
```

步骤：

```text
✓ 解析文档
✓ 提取与理解
✓ 生成 Change Set
● 等待用户确认
○ 应用到知识库
```

### 资料信息

* 类型；
* 语言；
* 页数；
* 文件大小；
* 导入时间；
* 模型。

### 技术详情

默认折叠：

* Job ID；
* Prepared ID；
* Source ID；
* Hash；
* 分块策略；
* 模型协议。

---

## 5. 底部操作栏

```text
[查看详情] [重试] [打开 PDF] [查看日志]     [应用确认]
```

只有等待确认状态显示“应用确认”。

---

# 六、模块三：审核 / 知识质量控制

## 1. 布局

```text
┌──────────────┬────────────────────────┬────────────────┐
│ 审核列表      │ 知识正文 / Diff          │ 证据 Inspector │
│ 300px         │ minmax(520px, 1fr)     │ 300px           │
└──────────────┴────────────────────────┴────────────────┘
```

底部接受操作栏固定。

---

## 2. 审核列表

顶部筛选：

```text
[全部] [论文] [教材] [主题] [概念] [更新建议]
[待审核 16] [已接受 8] [已拒绝 2]
```

每行：

```text
概念更新
倾向得分充分性
来源：Dragonnet 论文
高优先级 · 12 分钟前
```

或：

```text
论文片段
注意力机制的实现细节
来源：Attention Is All You Need
中优先级 · 25 分钟前
```

状态使用小标签，不在标题中展示：

```text
concept · accepted
```

---

## 3. 主内容区

顶部页签：

```text
预览   Diff   源文件   用户编辑
```

### 预览

渲染真实 Markdown：

```text
倾向得分充分性

定义
……

适用条件
……

数学表达
……
```

公式正常渲染。

### Diff

使用清晰新增、删除样式：

```diff
+ 新增：倾向得分的平衡性质
- 删除：过度泛化的模型结论
```

不要直接展示原始技术 JSON。

### 用户编辑

提供编辑器，但保存只是修改草稿，不自动接受。

---

## 4. 证据 Inspector

### 来源信息

```text
来源：Dragonnet 论文
第 12–14、17 页
[查看原文]
```

可显示 PDF 页面缩略图。

### Agent 理由

```text
该内容具备独立定义、跨论文复用性较高，适合作为概念笔记。
```

### 置信度

```text
92% · 高
```

### 相关知识

```text
倾向得分
无混杂性
IPW
协变量平衡
```

### 审核历史

时间线：

```text
10:30 提交待审
10:29 提取完成
10:27 从 PDF 第 12 页提取
```

---

## 5. 底部审核栏

固定：

```text
[拒绝] [稍后] [修改后接受]                  [接受 ▾]
```

接受下拉菜单：

* 接受；
* 接受并标记高优先级；
* 接受并加入今日学习；
* 接受并加入周末。

规则：

* 接受后即时从待审核列表移除；
* “修改后接受”使用用户当前编辑版本；
* 不允许模型重新覆盖；
* reviewed/core 后续只能生成更新建议。

---

# 七、模块四：计划 / 学习任务管理

## 1. 布局

计划页不使用右侧 Inspector，使用完整宽度看板：

```text
┌─────────────────────┬─────────────────────┬─────────────────────┐
│ 未来两天 / 队列      │ 本周计划             │ 已完成 / 延期        │
└─────────────────────┴─────────────────────┴─────────────────────┘
```

底部固定统计栏。

---

## 2. 页面顶部

```text
计划 · 学习计划

[主线 70%] [支线 30%]                         [计划设置]
本周预计 7 小时 20 分钟
```

---

## 3. 左列：近期队列

### 未来两天

```text
阅读论文：Transformer 架构演进
支线 · 90 分钟 · 明天
```

### 明天队列

每项：

```text
RLHF 技术原理与实现
主线 · 60 分钟
[开始]
```

支持：

* 上下调整；
* 移到今天；
* 移到周末；
* 稍后；
* 删除计划项。

### 周末队列

显示需要长时间完成的任务。

---

## 4. 中列：本周计划

按周一到周日分组：

```text
周一  3 项 · 2h30m
DeepSeek 论文研读              120 分钟  进行中
强化学习基础梳理                90 分钟  进行中

周二  2 项 · 1h40m
LLM 训练流程总结                60 分钟  待开始
论文阅读：Scaling Laws          45 分钟  待开始
```

每行右侧：

* 开始按钮；
* 状态；
* 更多菜单。

---

## 5. 右列：完成和延期

### 已完成

```text
✓ Transformer 基础概念梳理
  45 分钟 · 5 月 22 日
```

### 延期任务

```text
模型微调实践（LoRA）
原计划 5 月 20 日
[重新安排]
```

---

## 6. 底部统计栏

固定：

```text
本周任务数   8 项
预计分钟     720 分钟
已完成       315 分钟
完成比例     43.8%
连续学习     5 天
实际比例     68% / 32%
```

不能随着任务列表滚走。

---

# 八、模块五：助手 / AI 助手与模型配置

## 1. 布局

```text
┌────────────────────────────────────┬──────────────────────────┐
│ 聊天主区域                           │ 模型设置 Drawer           │
│ minmax(600px, 1fr)                  │ 320px                     │
└────────────────────────────────────┴──────────────────────────┘
```

模型 Drawer 默认关闭，点击“模型与 API 设置”打开。

---

## 2. 聊天顶部

```text
助手

[DeepSeek V4 Pro ▾] [新会话]

上下文：
[当前笔记：Dragonnet 论文]
[Dragonnet 来源 3]
[反向链接 8]

                         [模型与 API 设置]
```

上下文 chip 可移除、点击查看。

---

## 3. 消息区

只允许消息区滚动，顶部和输入区固定。

用户消息靠右，Agent 消息靠左。

Agent 消息底部：

```text
有帮助  没帮助  复制  更多
来源：Künzel et al., 2019
```

模型正在回答时：

* 流式输出；
* 停止按钮；
* 不重复滚动整个页面；
* 用户手动向上滚动后，不强制拉回底部。

---

## 4. 快捷命令

输入框上方：

```text
/解释当前笔记
/生成短测
/检查我的复述
/关联知识
/展开灵感
/生成更新建议
/生成周计划
```

输入 `/` 打开命令菜单。

输入 `@` 打开笔记选择器。

---

## 5. Composer

固定在底部：

```text
输入问题或指令，/ 唤起命令
```

底部操作：

* 添加上下文；
* 附件；
* 命令；
* 发送；
* 停止生成。

写入提示：

```text
所有写入操作将先创建 Change Set，确认后才应用到笔记。
```

---

## 6. 模型与 API 设置 Drawer

### Provider 列表

```text
DeepSeek       已配置 · 当前使用
OpenAI         已配置
Anthropic      未配置
Google         未配置
OpenAI Compatible
+ 添加提供商
```

实际第一版只要求稳定支持 OpenAI-compatible；其他可以作为预设。

### 配置表单

字段：

```text
配置名称
Provider 类型
Base URL
API Key
模型名称
获取模型列表
Temperature
Max Tokens
请求超时
流式输出
JSON Schema 支持
工具调用支持
```

API Key：

* 只显示掩码；
* 插件前端不持久化明文；
* 后端 Keychain 保存；
* 提供“替换 Key”，不提供明文回显。

### 模型路由

```text
助手模型             DeepSeek V4 Pro
PDF 整理模型          DeepSeek V4 Pro
审核辅助模型          DeepSeek V4 Pro
复习与小测模型        DeepSeek V4 Flash
周计划模型            DeepSeek V4 Pro
```

操作：

```text
[测试连接]
```

必须显示具体结果：

* 连接成功；
* Key 无效；
* Base URL 无效；
* 模型不存在；
* 请求超时；
* 服务不兼容。

---

# 九、按钮规范

当前按钮样式混乱，需要统一。

## 1. Primary

只用于页面唯一主要操作：

* 开始学习；
* 应用确认；
* 接受；
* 发送；
* 保存模型配置。

使用 Accent Color 实心背景。

## 2. Secondary

普通边框：

* 加入明天；
* 重试；
* 打开 PDF；
* 修改后接受。

## 3. Ghost

无背景：

* 刷新；
* 收藏；
* 更多；
* 返回；
* 稍后。

## 4. Destructive

只用于：

* 删除；
* 拒绝；
* 清空历史。

红色只在 hover 或确认状态明显出现，不能满页面使用粉红或红色标题。

---

# 十、标题和文字层级

避免当前页面标题过大、重复。

```text
产品标题           18–20px / semibold
模块标题           16–18px / semibold
内容标题           15–16px / semibold
正文               13–14px
辅助文字           11–12px
状态标签           10–11px
```

禁止：

* 页面上同时出现“知序”“学习 Agent”“推荐中心”“今日”四级大标题；
* 同一个标题重复出现；
* 粉红色作为默认标题颜色；
* 24px 以上的大面积标题。

---

# 十一、空状态

所有空状态必须可行动。

错误：

```text
未来两天
暂无安排
```

正确：

```text
未来两天暂无安排

从今日推荐中加入任务，或让 Agent 生成一个两天学习计划。

[生成计划]
```

资料：

```text
暂无待确认资料
[导入 PDF]
```

审核：

```text
所有草稿已经审核完毕
[查看已接受内容]
```

助手：

```text
尚未配置模型
[打开模型与 API 设置]
```

---

# 十二、加载和错误状态

## Loading

使用 skeleton，不要整页转圈。

## Error

显示：

```text
无法加载资料列表

本地 Agent 返回了无效响应。
错误代码：InvalidResponse

[重试] [查看详情] [打开日志]
```

不要显示 Python stack trace。

技术堆栈放在折叠详情。

---

# 十三、Codex 实现优先级

必须按顺序做，禁止同时铺开五页留下半成品。

### Phase 1：Shell 与滚动

* Global Header；
* Module Nav；
* 路由；
* 所有滚动归属；
* 响应式；
* 通用组件；
* 按钮与字体规范。

### Phase 2：今日

* 推荐列表；
* 详情；
* Inspector；
* 小知识；
* Study Session。

### Phase 3：资料

* 列表；
* Change Set；
* Inspector；
* 状态、错误和重试。

### Phase 4：审核

* 列表；
* Markdown；
* Diff；
* 证据；
* 审核操作。

### Phase 5：计划

* 近期任务；
* 本周计划；
* 完成与延期；
* 底部统计。

### Phase 6：助手

* Chat；
* Context；
* Provider Profiles；
* KeyStore；
* Model Routing。

每个 Phase 必须：

```text
实现
→ 测试
→ 截图
→ 与参考图对照
→ 修正
→ 再进入下一阶段
```

---

# 十四、硬性验收清单

Codex 不得宣称完成，除非以下全部满足：

* 页面外层无整体纵向滚动；
* 五个模块分别完成；
* 每个模块只有指定区域滚动；
* Header 不重复占用大量空间；
* 主导航不再使用顶部宽标签条；
* 今日右侧统计面板存在；
* 窄屏时统计面板转为 Drawer；
* 资料页有 Change Set 和状态 Inspector；
* 审核页有预览、Diff、证据 Inspector；
* 计划页底部统计固定；
* 助手输入区固定；
* 助手模型设置是 Drawer；
* API Key 不存前端明文；
* 所有内部 ID 默认隐藏；
* 标题不使用粉红色；
* applied、failed、running 状态准确；
* 所有空状态有操作入口；
* 所有错误有重试和日志；
* 深色和浅色主题均可用；
* 视觉截图分别覆盖五个模块；
* 不允许再用一张五宫格代替五张模块截图。

这份规范的关键是：**五个模块不是同一个模板换文字，而是五种不同的工作流布局；只有全局 Header、Module Nav、颜色和组件体系保持一致。**

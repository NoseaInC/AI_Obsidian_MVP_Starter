# Daily Intelligence V2 Specification

## 目标

今日页提供五类任务：必须复习、下一步学习、每日新知识、探索、资料任务。每日新知识必须由模型生成候选、与 Vault 去重并经过 A/B/C 机器验证；只有 A/B 进入该类别，C 只可作为探索或被排除。

## 每日排序

TypeScript 权威权重：到期 20%、路线 18%、知识缺口 18%、行为证据最高 15%、前置满足 12%、时间适配 7%、兴趣 5%、新颖性 5%。冷启动行为权重：少于 20 个事件最高 5%；20–100 最高 10%；超过 100 且覆盖 7 天为 15%。未使用的行为权重回流到路线相关性。

工作日每日新知识最多 1 个、探索最多 1 个；周末最多各 2 个。到期复习不被新知识挤掉。相同输入、日期与配置产生稳定顺序。

## 候选与验证

模型仅接收有界的正式知识标题、领域、mastery、importance 和 weak points，不接收完整 Vault、绝对路径或原始事件。模型不得伪造行为、来源或 URL。候选与正式知识、现有候选和别名执行本地标准化去重。

- A：至少两个本地正式知识证据、置信度不低于 0.88，核心目标一致。
- B：至少一个本地正式知识证据、置信度不低于 0.65，显示“来源有限，建议作为概念导读”。
- C：证据不足或不满足准入，不进入每日新知识。

## 学习闭环

每日新知识无需前置人工审核，可直接开始 Study Session。完成后记录结构化事件、实际时长和小测正确率，并自动加入下一次复习；不创建 reviewed/core，不静默修改 mastery。若存在正式来源笔记，mastery 建议仍需用户确认。

正式沉淀路径保持：Learning Cache → AI Draft → Change Set → 审核 → reviewed。

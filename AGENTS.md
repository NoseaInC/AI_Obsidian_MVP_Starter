# Vault 自动化永久约束

工程阶段、验收标准和当前恢复点见 `PROJECT_ROADMAP.md`、`PROJECT_STATUS.md`；重要取舍见 `DECISIONS.md`；专题规范索引见 `docs/README.md`。每次实现后运行 `./scripts/check.sh`，修复 P0/P1 后再推进下一阶段。

- 原始 PDF 只能在本地读取，不得上传；模型 API 只接收带 `PAGE` 标记的本地提取文本。
- 密钥只从环境变量读取，不得写入仓库、日志、Markdown、缓存或测试夹具。
- 自动化可以创建和更新本脚本同源的 `ai-draft`。
- `reviewed` 与 `core` 永远只读；发现同名或高度相似正式知识时，只创建更新建议。
- 知识选择优先学习主线主题和可跨来源复用的主线概念。
- 不得把论文专属模块、局部损失函数或作者命名组件默认提升为知识主线。
- 所有知识结论必须可追溯到来源；重要结论和概念必须携带来自 PDF 提取阶段的页码。
- 测试不得读取真实 PDF、调用真实模型 API 或依赖真实密钥。
- 自动化必须减少日常复制粘贴，不得把机器可完成的转录工作转嫁给用户。
- 所有写入必须先完成结构化校验和完整计划，使用临时文件与原子 rename，避免半成品。
- `90-Local-Only` 保存提取文本、原始模型 JSON、manifest 和更新建议，不把中间材料写进可同步知识目录。
